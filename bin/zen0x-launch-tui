#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an org.zen0x.COMMAND app id for styling.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.zen0x.$(basename $1) -e "$1" "${@:2}"
