[colors.primary]
background = "{{ bg0 }}"
foreground = "{{ fg0 }}"
dim_foreground = "{{ fg2 }}"
bright_foreground = "None"

[colors.cursor]
text = "{{ bg0 }}"
cursor = "{{ fg0 }}"

[colors.normal]
black = "{{ bg1 }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ blue }}"
magenta = "{{ purple }}"
cyan = "{{ cyan }}"
white = "{{ fg0 }}"

[colors.bright]
black = "{{ bg3 }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ accent }}"
magenta = "{{ purple }}"
cyan = "{{ cyan }}"
white = "{{ fg0 }}"

[colors.dim]
black = "{{ bg4 }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ blue }}"
magenta = "{{ purple }}"
cyan = "{{ cyan }}"
white = "{{ fg1 }}"
